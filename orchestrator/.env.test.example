# Copy to .env.test and fill in values. .env.test is not committed.
# Used by E2E tests (jest.e2e.config.js loads it via tests/setup-env.ts).

# Full E2E (E2E_FULL=1): real GitHub + Docker
GITHUB_TOKEN=
GITHUB_WEBHOOK_SECRET=
ALLOWED_REPOS=
PREVIEW_BASE_URL=

# DockerManager integration test (optional)
DOCKER_TEST_REPO_URL=https://github.com/danLeBrown/nestjs-project-preview-example